# LMS Backend (FastAPI)

## Quickstart

1. Create venv
```bash
uv venv
```

2. Install deps
```bash
uv sync
```

3. Run server
```bash
uv run uvicorn app.main:app --reload
```

## Env
Copy `.env.example` to `.env` and update values.
